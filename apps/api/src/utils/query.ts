export type QueryOptions={page:number;limit:number;search?:string;status?:string;sortBy:string;sortOrder:1|-1};
export function parseQueryOptions(query:Record<string,unknown>):QueryOptions{ const page=Math.max(1,Number(query.page??1)); const limit=Math.min(100,Math.max(1,Number(query.limit??20))); const sortBy=typeof query.sortBy==="string"?query.sortBy:"updatedAt"; const sortOrder=query.sortOrder==="asc"?1:-1; const search=typeof query.search==="string"?query.search:undefined; const status=typeof query.status==="string"?query.status:undefined; return {page,limit,search,status,sortBy,sortOrder}; }
