import { ReportModel } from "@jobflow/database/models";
import type { Request } from "express";
import { asyncHandler } from "../utils/asyncHandler";
import { paginatedResponse, successResponse } from "../utils/apiResponse";
import { ApiError } from "../utils/errors";
import { assertTenantId, buildTenantFilter, createTenantScopedRecord, findTenantScopedById, updateTenantScopedById } from "../services/baseTenant.service";
import { getPagination } from "../utils/pagination";
import { getDailyDigestMetrics, getReportHistoryStats, getWeeklyPerformanceMetrics } from "../services/report-analytics.service";
import { generateDailyDigest, generateWeeklyReport, sendReportTest } from "../services/report-generation.service";
import { enqueueAutomationModule } from "../services/automation-queue.service";
export const listReports = asyncHandler(async (req: Request, res) => { const tenantId=assertTenantId(req.tenantId); const {page,limit,skip}=getPagination(req.query); const search=typeof req.query.search==='string'?req.query.search:undefined; const status=typeof req.query.status==='string'?req.query.status:undefined; const filter:Record<string,unknown>=buildTenantFilter(tenantId); if(status) filter.status=status; if(search) filter.$or=[{name:{$regex:search,$options:'i'}},{type:{$regex:search,$options:'i'}}]; const [rows,total]=await Promise.all([ReportModel.find(filter).sort({updatedAt:-1}).skip(skip).limit(limit),ReportModel.countDocuments(filter)]); return paginatedResponse(res,rows,{page,limit,total,totalPages:Math.ceil(total/limit)}); });
export const createReport = asyncHandler(async (req: Request, res) => { const tenantId=assertTenantId(req.tenantId); const row=await createTenantScopedRecord(ReportModel,tenantId,req.user?.id??'system',req.body); return successResponse(res,row,'Created',201); });
export const getReportById = asyncHandler(async (req: Request, res) => { const tenantId=assertTenantId(req.tenantId); const row=await findTenantScopedById(ReportModel,tenantId,req.params.id); if(!row) throw new ApiError('Not found',404,'NOT_FOUND'); return successResponse(res,row); });
export const updateReport = asyncHandler(async (req: Request, res) => { const tenantId=assertTenantId(req.tenantId); const row=await updateTenantScopedById(ReportModel,tenantId,req.params.id,req.body); if(!row) throw new ApiError('Not found',404,'NOT_FOUND'); return successResponse(res,row,'Updated'); });
export const runDailyDigest = asyncHandler(async (req: Request, res) => { const tenantId=assertTenantId(req.tenantId); const userId=req.user?.id ?? "system"; const execute=req.query.execute==="true"; if(execute){ if(process.env.NODE_ENV==="production") throw new ApiError("Direct execute=true is disabled in production",403,"FORBIDDEN"); const result = await generateDailyDigest({ tenantId, userId, date:req.body.date, send:req.body.send, force:req.body.force }); return successResponse(res,result,'Daily digest generated'); } const queued=await enqueueAutomationModule({ tenantId, userId, moduleKey:"daily-digest", payload:{ date:req.body.date, send:req.body.send, force:req.body.force }, source:"api" }); return successResponse(res,{operationId:queued.operationId,jobId:queued.jobId,moduleKey:"daily-digest",status:queued.status,message:queued.message},'Daily digest queued'); });
export const runWeeklyReport = asyncHandler(async (req: Request, res) => { const tenantId=assertTenantId(req.tenantId); const userId=req.user?.id ?? "system"; const execute=req.query.execute==="true"; if(execute){ if(process.env.NODE_ENV==="production") throw new ApiError("Direct execute=true is disabled in production",403,"FORBIDDEN"); const result = await generateWeeklyReport({ tenantId, userId, weekStart:req.body.weekStart, weekEnd:req.body.weekEnd, send:req.body.send, force:req.body.force }); return successResponse(res,result,'Weekly report generated'); } const queued=await enqueueAutomationModule({ tenantId, userId, moduleKey:"weekly-report", payload:{ weekStart:req.body.weekStart, weekEnd:req.body.weekEnd, send:req.body.send, force:req.body.force }, source:"api" }); return successResponse(res,{operationId:queued.operationId,jobId:queued.jobId,moduleKey:"weekly-report",status:queued.status,message:queued.message},'Weekly report queued'); });
export const sendTestReport = asyncHandler(async (req: Request, res) => { const tenantId=assertTenantId(req.tenantId); const result = await sendReportTest({ tenantId, reportId:req.params.id, userId:req.user?.id ?? "system", to:req.body?.to }); return successResponse(res,result,'Report test sent'); });
export const getReportStats = asyncHandler(async (req: Request, res) => { const tenantId=assertTenantId(req.tenantId); const stats=await getReportHistoryStats({tenantId}); return successResponse(res,stats); });
export const getDailyAnalytics = asyncHandler(async (req: Request, res) => { const tenantId=assertTenantId(req.tenantId); const date = typeof req.query.date === "string" ? new Date(req.query.date) : new Date(); const metrics=await getDailyDigestMetrics({tenantId,date}); return successResponse(res,metrics); });
export const getWeeklyAnalytics = asyncHandler(async (req: Request, res) => { const tenantId=assertTenantId(req.tenantId); const now = new Date(); const weekStart = typeof req.query.weekStart === "string" ? new Date(req.query.weekStart) : new Date(now.setDate(now.getDate() - (now.getDay() || 7) + 1)); const weekEnd = typeof req.query.weekEnd === "string" ? new Date(req.query.weekEnd) : new Date(new Date(weekStart).setDate(weekStart.getDate() + 6)); const metrics=await getWeeklyPerformanceMetrics({tenantId,weekStart,weekEnd}); return successResponse(res,metrics); });
