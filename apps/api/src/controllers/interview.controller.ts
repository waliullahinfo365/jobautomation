import { InterviewModel } from "@database/models";
import type { Request } from "express";
import { asyncHandler } from "../utils/asyncHandler";
import { paginatedResponse, successResponse } from "../utils/apiResponse";
import { ApiError } from "../utils/errors";
import { logApiAction } from "../services/automation-log.service";
import { assertTenantId, buildTenantFilter, createTenantScopedRecord, findTenantScopedById, updateTenantScopedById } from "../services/baseTenant.service";
import { getPagination } from "../utils/pagination";
import { createInterviewCalendarEvent } from "../services/interview-scheduling.service";
import { enqueueAutomationModule } from "../services/automation-queue.service";
export const listInterviews = asyncHandler(async (req: Request, res) => { const tenantId=assertTenantId(req.tenantId); const {page,limit,skip}=getPagination(req.query); const search=typeof req.query.search==='string'?req.query.search:undefined; const status=typeof req.query.status==='string'?req.query.status:undefined; const filter:Record<string,unknown>=buildTenantFilter(tenantId); if(status) filter.status=status; if(search) filter.$or=[{company:{$regex:search,$options:'i'}},{position:{$regex:search,$options:'i'}},{interviewerName:{$regex:search,$options:'i'}},{contactEmail:{$regex:search,$options:'i'}}]; const [rows,total]=await Promise.all([InterviewModel.find(filter).sort({dateTime:1}).skip(skip).limit(limit),InterviewModel.countDocuments(filter)]); return paginatedResponse(res,rows,{page,limit,total,totalPages:Math.ceil(total/limit)}); });
export const createInterview = asyncHandler(async (req: Request, res) => { const tenantId=assertTenantId(req.tenantId); const row=await createTenantScopedRecord(InterviewModel,tenantId,req.user?.id??'system',req.body); return successResponse(res,row,'Created',201); });
export const getInterviewById = asyncHandler(async (req: Request, res) => { const tenantId=assertTenantId(req.tenantId); const row=await findTenantScopedById(InterviewModel,tenantId,req.params.id); if(!row) throw new ApiError('Not found',404,'NOT_FOUND'); return successResponse(res,row); });
export const updateInterview = asyncHandler(async (req: Request, res) => { const tenantId=assertTenantId(req.tenantId); const row=await updateTenantScopedById(InterviewModel,tenantId,req.params.id,req.body); if(!row) throw new ApiError('Not found',404,'NOT_FOUND'); return successResponse(res,row,'Updated'); });
export const createCalendarEvent = asyncHandler(async (req: Request, res) => { const tenantId=assertTenantId(req.tenantId); const interviewId=req.params.id; const userId=req.user?.id ?? "system"; const execute=req.query.execute==="true"; if(execute){ if(process.env.NODE_ENV==="production") throw new ApiError("Direct execute=true is disabled in production",403,"FORBIDDEN"); const result=await createInterviewCalendarEvent({ tenantId, interviewId, userId }); await logApiAction({tenantId,moduleKey:'interview-scheduling',status:'Success',message:'Calendar event created',relatedRecordType:'Interview',relatedRecordId:req.params.id,metadata:{operationId:result.operationId,calendarEventId:result.calendarEventId}}); return successResponse(res,result,'Calendar event created'); } const queued=await enqueueAutomationModule({ tenantId, userId, moduleKey:"interview-scheduling", payload:{ interviewId }, source:"api" }); return successResponse(res,{operationId:queued.operationId,jobId:queued.jobId,moduleKey:"interview-scheduling",status:queued.status,message:queued.message},'Calendar event queued'); });
export const markComplete = asyncHandler(async (req: Request, res) => { const tenantId=assertTenantId(req.tenantId); const row=await updateTenantScopedById(InterviewModel,tenantId,req.params.id,{status:'Completed'} as never); if(!row) throw new ApiError('Not found',404,'NOT_FOUND'); return successResponse(res,row,'Interview marked complete'); });
