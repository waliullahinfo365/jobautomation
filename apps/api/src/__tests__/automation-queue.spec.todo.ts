/**
 * Queue foundation test scaffold.
 *
 * TODO cases:
 * - enqueue returns operationId/jobId
 * - duplicate idempotency key returns skipped/existing
 * - automation run endpoint queues by default
 * - execute=true blocked in production
 */
