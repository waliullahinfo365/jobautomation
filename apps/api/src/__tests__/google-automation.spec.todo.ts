/**
 * Phase 5 Google automation test scaffold.
 *
 * TODO cases:
 * - provisioning creates folder records once
 * - repeated provisioning returns existing folder result
 * - CV routing provisions folder if missing
 * - repeated CV routing does not duplicate routing results
 * - PDF export returns existing PDF if already exported
 * - calendar event creation is idempotent
 * - all operations are tenant-scoped
 */
