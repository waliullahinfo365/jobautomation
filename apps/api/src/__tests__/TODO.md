# API Test TODOs

- tenant middleware rejects missing `x-tenant-id` for protected routes
- health route works without tenant header
- base tenant filter includes tenantId
- archive route is tenant-scoped
- automation run placeholder returns `{ operationId, moduleKey, status: "queued" }`
