/**
 * TODO: Convert to executable tests once test runner is finalized.
 *
 * Cases:
 * 1) mark applied updates application and related job
 * 2) mark applied auto-schedules follow-up date (+5 days default)
 * 3) repeated mark applied does not duplicate side effects
 * 4) process due follow-ups marks reminders as sent exactly once
 * 5) reply-test classifies interview intent and updates application/job
 * 6) repeated reply providerMessageId is skipped
 * 7) tenant scoping blocks cross-tenant updates
 */

export {};
