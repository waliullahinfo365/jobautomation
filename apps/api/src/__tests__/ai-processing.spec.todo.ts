/**
 * TODO: Add executable tests once the runner is finalized.
 *
 * Cases:
 * 1) generate research creates exactly one Research document
 * 2) repeated generate research does not create duplicate document
 * 3) generate draft creates one Cover Letter draft document
 * 4) full processing creates both research + draft documents
 * 5) missing job returns 404
 * 6) generated documents are tenant-scoped
 * 7) automation logs are created for success/failure flows
 */

export {};
