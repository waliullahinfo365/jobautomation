/**
 * Phase 6 reports test scaffold.
 *
 * TODO cases:
 * - daily digest creates one report per tenant/day
 * - repeated daily digest returns existing unless force=true
 * - weekly report creates one report per tenant/week
 * - report send-test updates delivery status
 * - analytics are tenant-scoped
 * - failed generation logs AutomationLog failed
 * - automation module run execute=true works
 */
