/**
 * TODO: Add executable tests once test runner is finalized.
 *
 * Pseudo test cases:
 * 1) fingerprint utility returns same hash for equivalent inputs
 * 2) duplicate-protection marks exact fingerprint as Duplicate
 * 3) duplicate-protection marks same company + similar title as Possible Duplicate
 * 4) processJobIntakeEmail creates a Job for Unique payloads
 * 5) processJobIntakeEmail skips repeated providerMessageId for same tenant
 * 6) POST /jobs/intake-test succeeds with x-tenant-id header
 */

export {};
